# CT-57 Test Notes

## Test files modified

### 1. `src/test/services/line_persistence_service_test.dart`

Added `group('rerouteLine', ...)` with 9 test cases covering the repository-level `rerouteLine` method:

| # | Test | Covers |
|---|------|--------|
| 1 | basic reroute: re-parents children and prunes old path | AC: children re-parented, orphaned path pruned, review cards preserved |
| 2 | reroute with empty newMoves (convergence node already exists) | Re-parent + prune without inserting new moves |
| 3 | reroute with buffered moves to persist: inserts chain and re-parents | AC: buffered moves persisted up to convergence point |
| 4 | no SAN conflict when new parent has no children | Verifies reroute to fresh node succeeds |
| 5 | SAN conflict at DB level: reroute fails when duplicate SAN exists | AC: SAN conflicts block the reroute |
| 6 | pruning stops at branching ancestor | AC: orphaned path pruned only to nearest branching node |
| 7 | pruning stops at node with review card | Pruning respects review card presence |
| 8 | label updates applied atomically | AC: atomic transaction includes label updates |
| 9 | review cards preserved after reroute: leaf cards unchanged | AC: review cards on re-parented leaves continue to function |

### 2. `src/test/controllers/add_line_controller_test.dart`

Added `group('Reroute', ...)` with 6 test cases covering `performReroute` and `getRerouteInfo`:

| # | Test | Covers |
|---|------|--------|
| 1 | reroute succeeds and rebuilds state: transpositionMatches clears | AC: tree cache rebuilt, transposition warning dismisses |
| 2 | reroute with SAN conflict returns RerouteConflict | AC: SAN conflicts detected and block reroute |
| 3 | reroute with buffered moves persists only prefix up to focusedPillIndex | Post-convergence buffered moves discarded |
| 4 | reroute preserves review cards on leaf moves | AC: review cards preserved |
| 5 | state reloads correctly after reroute: pills, FEN, displayName consistent | State consistency after reload |
| 6 | getRerouteInfo returns 0 continuationLineCount for leaf match | Leaf match handled gracefully |

### 3. `src/test/screens/add_line_screen_test.dart`

Added `group('CT-57: Reroute', ...)` with 7 widget test cases:

| # | Test | Covers |
|---|------|--------|
| 1 | Reroute button is tappable for same-opening non-leaf matches | AC: Reroute button appears on each row |
| 2 | Reroute button is NOT shown for leaf matches | Leaf exclusion (no children to reroute) |
| 3 | tapping Reroute shows confirmation dialog | AC: confirmation dialog with details |
| 4 | cancelling the dialog leaves everything unchanged | Dialog cancel preserves state |
| 5 | confirming the dialog performs reroute and shows snackbar | AC: reroute executes, snackbar shown |
| 6 | Reroute button not shown for cross-opening matches | Cross-opening exclusion |
| 7 | existing Reroute button test updated: requires non-leaf match | Verifies updated visibility condition |

## Acceptance criteria coverage

| Acceptance Criterion | Test Coverage |
|---------------------|---------------|
| Reroute button appears on each row in the transposition warning | Widget test #1, #7 |
| Tapping Reroute shows a confirmation dialog | Widget test #3 |
| On confirm, buffered moves are persisted up to the convergence point | Repo test #3, Controller test #3 |
| Children of old convergence node re-parented under new convergence node | Repo test #1, #2, #3 |
| Orphaned old path pruned back to nearest branching ancestor | Repo test #1, #6, #7 |
| Review cards on re-parented leaves continue to function | Repo test #9, Controller test #4 |
| Tree cache rebuilt after reroute | Controller test #1, #5 |
| Transposition warning dismisses after successful reroute | Controller test #1, Widget test #5 |
| SAN conflicts detected and block the reroute | Repo test #5, Controller test #2 |
| Atomic operation (all-or-nothing within a single transaction) | Repo test #8 (label updates in same tx) |
| Unit tests for reroute logic | All repo and controller tests |
| Widget test for confirmation dialog flow | Widget tests #3, #4, #5 |

## Acceptance criteria NOT automatically testable

| Criterion | Reason |
|-----------|--------|
| Update `features/add-line.md` with Reroute subsection | Spec file update -- verified by manual review |
| Update `features/line-management.md` with Rerouting subsection | Spec file update -- verified by manual review |
| Update `architecture/repository.md` with new repository method | Spec file update -- verified by manual review |
