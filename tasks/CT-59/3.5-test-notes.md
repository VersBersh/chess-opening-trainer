# CT-59: Test Notes

## Test files modified

| File | Tests added |
|------|-------------|
| `src/test/controllers/add_line_controller_test.dart` | 13 new tests in 5 groups |
| `src/test/screens/add_line_screen_test.dart` | 5 new testWidgets in 1 group |

## Controller tests (`add_line_controller_test.dart`)

### Group: `CT-59: hasPendingLabelChanges and hasUnsavedChanges`

| Test | Covers |
|------|--------|
| `hasPendingLabelChanges is false with no label edits` | Baseline: new getters return false when no edits exist |
| `hasPendingLabelChanges is true after updateLabel` | `hasPendingLabelChanges` getter, `hasUnsavedChanges` unifies both change types |
| `hasUnsavedChanges is true when hasNewMoves is true but no pending labels` | `hasUnsavedChanges` still works for the existing new-move path |
| `hasUnsavedChanges is true when both new moves and pending labels exist` | Both change types contribute to `hasUnsavedChanges` |

### Group: `CT-59: confirmAndPersist with label-only edits`

| Test | Covers |
|------|--------|
| `confirmAndPersist persists label-only edits when no new moves` | **AC: Confirm button enabled with pending labels; label-only edits persisted on Confirm** -- verifies DB write, ConfirmSuccess result with empty insertedMoveIds, pendingLabels cleared |
| `confirmAndPersist returns ConfirmNoNewMoves when no changes at all` | Guard: no new moves AND no pending labels still returns ConfirmNoNewMoves |
| `label-only confirm preserves focusedPillIndex and currentFen` | In-place state refresh: focused pill and board FEN preserved (not reset to leaf) |
| `label-only confirm increments undoGeneration` | Stale undo snackbar invalidation |
| `label-only confirm persists multiple label changes in one confirm` | Batch persistence: multiple pending labels written atomically |
| `existing new-move confirm flow is unchanged (regression)` | **AC: Existing behavior for new-move confirms is unchanged** |

### Group: `CT-59: flipAndConfirm with label-only edits`

| Test | Covers |
|------|--------|
| `flipAndConfirm persists label-only edits when no new moves` | Label-only path also works via flipAndConfirm (parity validation is irrelevant) |
| `flipAndConfirm returns ConfirmNoNewMoves when no changes at all` | Guard parity with confirmAndPersist |

### Group: `CT-59: isExistingLine with pending label changes`

| Test | Covers |
|------|--------|
| `isExistingLine is false when hasPendingLabelChanges is true` | "Existing line" info label hides when labels have been edited |

### Group: `CT-59: stale undo invalidation after label-only confirm`

| Test | Covers |
|------|--------|
| `label-only confirm after a new-move confirm invalidates prior undo` | Undo from first confirm becomes no-op after a second (label-only) confirm increments the generation |

## Widget tests (`add_line_screen_test.dart`)

### Group: `CT-59: Label-only confirm flow`

| Test | Covers |
|------|--------|
| `confirm enabled and persists label-only edits` | **AC: Confirm button is enabled when there are pending label edits; label-only edits persisted on Confirm** -- full widget flow: edit label, verify button enables, tap Confirm, verify DB, verify button disables |
| `label-only confirm preserves focused pill and board position` | **AC (from plan step 11b):** focused pill and board FEN preserved after label-only save |
| `discard dialog shown when only pending labels exist` | **AC (implicit via plan step 9):** PopScope canPop is false when pending label edits exist |
| `no undo snackbar shown for label-only confirm` | Verifies ConfirmSuccess with empty insertedMoveIds correctly skips undo snackbar |
| `existing new-move confirm flow still works after label-only changes (regression)` | **AC: Existing behavior for new-move confirms is unchanged** |

## Acceptance criteria coverage

| Criterion | Tested? | Notes |
|-----------|---------|-------|
| Confirm button is enabled when there are pending label edits, even with no new moves | Yes | Controller: `hasPendingLabelChanges/hasUnsavedChanges` tests; Widget: `confirm enabled and persists label-only edits` |
| Label-only edits are persisted when Confirm is tapped | Yes | Controller: `confirmAndPersist persists label-only edits`; Widget: `confirm enabled and persists label-only edits` |
| Existing behavior for new-move confirms is unchanged | Yes | Controller + Widget regression tests |
| Widget test covering label-only confirm flow | Yes | Full group of 5 widget tests |

## Acceptance criteria that could not be tested automatically

None -- all acceptance criteria are covered by the automated tests above.
