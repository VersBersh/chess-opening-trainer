- **Verdict** — `Approved with Notes`

- **Issues**
  1. **Major — Dependency Inversion / Single Responsibility:** [`add_line_controller.dart#L1`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L1) and [`add_line_controller.dart#L852`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L852) push board-rendering concerns into the controller by importing `dart:ui`, `chessground` types, and returning `ISet<Shape>` built from `Arrow` + `Color`. The controller already owns loading, move-entry, persistence, undo, and label editing; this diff adds presentation rendering on top. It also creates direct coupling from high-level add-line logic to a specific board library. That makes the design harder to extend if arrow styling/rendering changes, and it duplicates move-to-arrow translation concerns that already exist in [`repertoire_browser_controller.dart#L267`](C:/code/draftable/chess-3/src/lib/controllers/repertoire_browser_controller.dart#L267). Suggested fix: extract hint-arrow resolution into a dedicated pure service/helper that returns domain descriptors (`from`, `to`, `kind: direct|transposition`), then map those to `Arrow`/`Color` in the screen or a presentation adapter.

  2. **Major — Open/Closed / Hidden Coupling:** adding `showHintArrows` to [`AddLineState#L25`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L25) required threading the field through every full-state reconstruction site, e.g. [`#L221`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L221), [`#L441`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L441), [`#L545`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L545), [`#L847`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L847), [`#L943`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L943). That is brittle temporal coupling: every future state field now depends on every mutation method being updated correctly. This change worked, but the design is not closed for extension and is easy to break with silent field loss. Suggested fix: add `copyWith` to `AddLineState`, or split persistent domain state from view preferences so move/persistence transitions do not need to manually preserve unrelated UI flags.

  3. **Minor — Clean Code / File Size / Embedded Design Principle:** the modified files are all well past the 300-line smell threshold: [`add_line_controller.dart`](C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart) is 947 lines, [`add_line_screen.dart`](C:/code/draftable/chess-3/src/lib/screens/add_line_screen.dart) is 792 lines, [`add_line_controller_test.dart#L2736`](C:/code/draftable/chess-3/src/test/controllers/add_line_controller_test.dart#L2736) is 3132 lines, and [`add_line_screen_test.dart#L2390`](C:/code/draftable/chess-3/src/test/screens/add_line_screen_test.dart#L2390) is 2565 lines. This diff continues adding responsibilities to already oversized modules instead of carving out clearer boundaries. That hurts readability and makes the architecture harder to infer from code alone. Suggested fix: extract hint-arrow logic into its own unit-tested module, extract the app-bar/board section from the screen, and split the new tests into dedicated hint-arrow test files.