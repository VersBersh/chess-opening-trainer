- **Verdict** — `Needs Fixes`
- **Issues**
  1. **Major — Single Responsibility / Hidden Coupling:** [`move_pills_widget.dart`](C:/code/draftable/chess-2/src/lib/widgets/move_pills_widget.dart#L22) and [`move_pills_widget.dart`](C:/code/draftable/chess-2/src/lib/widgets/move_pills_widget.dart#L88) now bake the Add Line screen’s board-to-pill spacing rule into `MovePillsWidget` itself. The comment and constant explicitly depend on a board sitting above the widget, but the widget’s stated responsibility is just “renders moves as a wrapping row of tappable pills.” That creates semantic coupling to [`add_line_screen.dart`](C:/code/draftable/chess-2/src/lib/screens/add_line_screen.dart#L433), and the widget will carry an unexplained 18 dp top gap anywhere else it is reused. This matters because parent-layout spacing is a screen concern, while intra-pill row spacing is a pill-widget concern. Suggested fix: keep `_kPillRunSpacing` inside `MovePillsWidget`, but move the board-to-first-row gap to `AddLineScreen` or pass it as an explicit parameter such as `topPadding`/`outerPadding` so the dependency is visible at the composition boundary.
  2. **Major — Clean Code / Embedded Design Principle:** The new test in [`move_pills_widget_test.dart`](C:/code/draftable/chess-2/src/test/widgets/move_pills_widget_test.dart#L286) is named and documented as verifying “board-to-first-row gap,” but it does not render a board or the real Add Line layout. It measures the top of the widget’s own `Padding` against `GestureDetector` positions inside `MovePillsWidget`, so it only proves an internal implementation detail of the widget, not the screen-level contract from the spec. That gives false confidence and tightly couples the test to current structure (`Padding`, `Wrap`, `GestureDetector`) rather than behavior. Suggested fix: move this assertion to [`add_line_screen_test.dart`](C:/code/draftable/chess-2/src/test/screens/add_line_screen_test.dart) and measure the actual board bottom versus first pill body, or rename/re-scope the widget test so it honestly verifies internal top padding rather than the board-spacing requirement.
  3. **Minor — File Size / Single Responsibility:** [`move_pills_widget_test.dart`](C:/code/draftable/chess-2/src/test/widgets/move_pills_widget_test.dart) is now 506 lines. Per the stated review criteria, that is a code smell. The file mixes rendering, semantics, layout, interaction, fallback-theme, and sizing concerns in one place, which makes the design intent harder to scan and increases maintenance cost. Suggested fix: split it into focused test files or groups, for example widget rendering/interaction vs layout/semantics, and move Add Line integration assertions to screen-level tests where they belong.