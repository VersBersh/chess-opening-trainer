- **Verdict** — `Approved with Notes`
- **Issues**
1. **Major — Hidden/Semantic Coupling + Primitive Obsession:** The label position is coupled to a magic number (`_kLabelBottomOffset = -14`) and a hardcoded label font size (`fontSize: 10`), so changing typography or text scale can break spacing/overlap behavior. See [src/lib/widgets/move_pills_widget.dart#L8](/C:/code/misc/chess-trainer-3/src/lib/widgets/move_pills_widget.dart#L8) and [src/lib/widgets/move_pills_widget.dart#L189](/C:/code/misc/chess-trainer-3/src/lib/widgets/move_pills_widget.dart#L189). This weakens SRP/OCP because layout correctness depends on unrelated style constants. Suggested fix: derive offset from measured text metrics or use a layout that computes label placement from actual child size/text scale (or at minimum centralize both offset and text style in one tokenized config object).
2. **Minor — Test Design / Naming Intent:** The “no label when null” test infers “no label” by checking absence of any `Text` with `fontSize == 10`, which is brittle and not intent-revealing. See [src/test/widgets/move_pills_widget_test.dart#L191](/C:/code/misc/chess-trainer-3/src/test/widgets/move_pills_widget_test.dart#L191). This creates semantic coupling to a style detail rather than behavior. Suggested fix: assert absence of the expected label text for that pill (or scope finder to the specific pill subtree with a dedicated key/semantics marker).
3. **Minor — File Size Smell (Clean Code):** The modified test file is now over 300 lines (329), which increases cognitive load and weakens architectural readability. See [src/test/widgets/move_pills_widget_test.dart#L1](/C:/code/misc/chess-trainer-3/src/test/widgets/move_pills_widget_test.dart#L1). Suggested fix: split into focused groups/files (layout/label behavior vs color/state behavior vs fallback behavior).