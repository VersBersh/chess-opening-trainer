- **Verdict** — `Approved with Notes`
- **Issues**
  1. **Minor — Hidden Coupling / Embedded Design Principle**: In [`src/lib/screens/add_line_screen.dart`](C:\code\misc\chess-trainer-2\src\lib\screens\add_line_screen.dart:457), the new comment explains orientation independence but removed the prior rationale for `!_controller.hasNewMoves`. That guard exists because `_controller.updateLabel()` reloads state and can discard buffered moves. Without that explanation, the temporal/data coupling is less discoverable to future maintainers. Suggested fix: keep the new orientation note, but also retain a one-line explanation of why unsaved moves disable label editing.
  2. **Minor — Clean Code (File Size / SRP in Tests)**: [`src/test/screens/add_line_screen_test.dart`](C:\code\misc\chess-trainer-2\src\test\screens\add_line_screen_test.dart) is now 472 lines, exceeding the 300-line smell threshold, and the new tests add more repeated setup/interaction patterns. Suggested fix: split by concern (e.g., `add_line_screen_label_test.dart`, `add_line_screen_navigation_test.dart`) and/or extract shared helpers for “seed + start at saved pill + assert label enabled.”