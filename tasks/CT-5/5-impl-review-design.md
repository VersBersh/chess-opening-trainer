- **Verdict** — `Needs Fixes`
- **Issues**
1. **Major — Hidden Semantic Coupling / OCP risk**: Quality aggregation in [`drill_screen.dart:324`](C:/code/misc/chess-trainer-1/src/lib/screens/drill_screen.dart:324) hard-codes scheduler semantics (`5/4/2/default`) inside UI-controller code. This duplicates domain policy from SM-2 mapping and will silently drift if quality rules change. **Why it matters:** one policy change would require touching multiple modules (scheduler + screen controller), violating Open/Closed and increasing regression risk. **Suggested fix:** centralize “quality -> bucket” mapping in a shared domain abstraction (enum/value object or scheduler helper), and consume that abstraction in `DrillController` instead of raw ints.

2. **Major — Single Responsibility violation / oversized production file**: [`drill_screen.dart`](C:/code/misc/chess-trainer-1/src/lib/screens/drill_screen.dart) is now ~690 lines and combines state-machine orchestration, session analytics accumulation, UI composition, and formatting helpers (`_formatDuration`, `_formatNextDue`, `_buildBreakdownRow`). **Why it matters:** high change-coupling; UI tweaks and drill-domain changes collide in one unit, making maintenance and review harder. **Suggested fix:** extract at least `SessionSummary` + aggregation logic into a separate domain/service class, and move summary rendering into a dedicated widget file.

3. **Minor — Temporal coupling / testability**: Direct `DateTime.now()` calls in [`drill_screen.dart:172`](C:/code/misc/chess-trainer-1/src/lib/screens/drill_screen.dart:172), [`drill_screen.dart:360`](C:/code/misc/chess-trainer-1/src/lib/screens/drill_screen.dart:360), and [`drill_screen.dart:677`](C:/code/misc/chess-trainer-1/src/lib/screens/drill_screen.dart:677) create hidden runtime dependencies on wall-clock time. **Why it matters:** behavior becomes non-deterministic at boundaries/time zones and forces weak tests (“text exists” instead of exact behavior). **Suggested fix:** inject a clock abstraction (or `DateTime Function()`), and use it for duration and relative-date formatting.

4. **Minor — Oversized test file / DRY pressure**: [`drill_screen_test.dart`](C:/code/misc/chess-trainer-1/src/test/screens/drill_screen_test.dart) is ~808 lines and repeats the same “complete single card” flow in multiple new tests (for breakdown/duration/next due). **Why it matters:** duplicated setup raises maintenance cost and increases brittle test drift. **Suggested fix:** extract helper(s) for “pump drill and complete card” and reuse across the session-summary tests.