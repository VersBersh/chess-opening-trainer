# CT-63: Test Notes

## Test files modified

- `src/test/screens/home_screen_test.dart` -- added four new test groups (24 tests total)

## What each test covers

### Group: `HomeScreen - create repertoire dialog` (7 tests)

| Test | Acceptance criteria |
|------|---------------------|
| Create button is enabled when valid name is entered | Dialog with name input; validation |
| Create button is disabled when name is empty | Validation: prevent empty names |
| Create button is disabled when name is whitespace-only | Validation: prevent empty names |
| Create button is disabled when name exceeds 100 characters | Validation: max length |
| Duplicate name shows error and disables Create button | Validation: handle duplicates gracefully |
| Duplicate check is case-insensitive | Validation: handle duplicates gracefully |
| Creating a repertoire adds it to the list | List updates after each operation |

### Group: `HomeScreen - rename repertoire dialog` (8 tests)

| Test | Acceptance criteria |
|------|---------------------|
| Rename dialog opens from context menu with pre-filled name | Rename dialog accessible from context menu |
| Rename button is disabled when name is empty | Validation: prevent empty names |
| Rename button is disabled when name is whitespace-only | Validation: prevent empty names |
| Rename button is disabled when name exceeds 100 characters | Validation: max length |
| Rename button is disabled for duplicate name | Validation: handle duplicates gracefully |
| Renaming to the same name is allowed (no error) | Duplicate check excludes current name |
| Successful rename updates the list | List updates after each operation |
| Cancel does not rename | Dialog cancel behaviour |

### Group: `HomeScreen - delete repertoire dialog` (3 tests)

| Test | Acceptance criteria |
|------|---------------------|
| Delete dialog opens from context menu with correct warning | Delete dialog with confirmation warning (mentions lines and review cards) |
| Confirming delete removes the repertoire from the list | List updates after each operation; transitions to empty state when last deleted |
| Cancelling delete keeps the repertoire | Dialog cancel behaviour |

### Group: `HomeScreen - repertoire list` (6 tests)

| Test | Acceptance criteria |
|------|---------------------|
| Multiple repertoires are displayed with due counts | Repertoire list on home screen; multi-repertoire layout |
| Each repertoire has action buttons | Per-repertoire action buttons (Start Drill, Free Practice, Add Line, Manage Repertoire) |
| FAB is visible when repertoires exist | Create repertoire dialog accessible via FAB |
| FAB is not visible in empty state | Empty state shows onboarding instead of FAB |
| Each repertoire card has a PopupMenuButton | Context menu for rename/delete |
| Repertoire cards are rendered as Card widgets | Card-per-repertoire layout |

## Acceptance criteria not tested automatically

All acceptance criteria from `task.md` are covered by the tests above. No criteria were identified that require manual testing.

Note: These tests are written against the interfaces described in the plan. They will not compile or pass until the implementation (Steps 2-4 of the plan) is complete. Some existing tests in the file (e.g., `does not show Card or FloatingActionButton`) will need to be updated or removed during implementation, as noted in plan Step 9.
