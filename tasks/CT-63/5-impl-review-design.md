- **Verdict** — `Needs Fixes`
- **Issues**
  1. **Critical — Hidden coupling / control flow bug:** In [src/lib/screens/home_screen.dart](C:/code/draftable/chess-2/src/lib/screens/home_screen.dart#L363), the `PopupMenuButton` dispatch uses a `switch` whose cases do not terminate. In Dart, a non-empty `case` cannot implicitly fall through, so this is a compile-time blocker; and if “fixed” by allowing fallthrough semantics later, selecting `Rename` would also run delete logic. This is not just a syntax problem, it means the menu action contract is not encoded safely. Fix by using explicit termination (`return`/`break`) and preferably replacing raw string cases with a small enum or separate callbacks so the UI action mapping is typed rather than string-coupled.

  2. **Major — Single Responsibility / DRY / Open for extension:** `_HomeScreenState` in [src/lib/screens/home_screen.dart](C:/code/draftable/chess-2/src/lib/screens/home_screen.dart#L23) is now 407 lines and owns navigation, CRUD orchestration, dialog construction, validation policy, and card rendering. The create and rename dialogs at [src/lib/screens/home_screen.dart](C:/code/draftable/chess-2/src/lib/screens/home_screen.dart#L73) and [src/lib/screens/home_screen.dart](C:/code/draftable/chess-2/src/lib/screens/home_screen.dart#L122) duplicate nearly the same validation and widget structure. That gives the screen multiple reasons to change and means any future rule change to repertoire naming has to be edited in multiple places. Fix by extracting a reusable `RepertoireNameDialog` or `showRepertoireNameDialog(...)` with shared normalization/duplicate-check logic, and by moving the card UI into a dedicated `RepertoireCard` widget.

  3. **Minor — File size / Embedded design clarity:** [src/test/screens/home_screen_test.dart](C:/code/draftable/chess-2/src/test/screens/home_screen_test.dart#L1) is 1312 lines, and this change continues to concentrate fake repositories, app wiring, navigation tests, dialog tests, and layout tests in one file. That makes the test architecture hard to understand from code alone and raises the cost of localized change. Fix by splitting shared fakes/builders into test helpers and separating home-screen layout tests from CRUD-dialog tests. Also note that [src/lib/screens/home_screen.dart](C:/code/draftable/chess-2/src/lib/screens/home_screen.dart#L1) itself is over the 300-line threshold, which reinforces the extraction suggested above.