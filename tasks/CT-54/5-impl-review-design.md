**Verdict** — `Needs Fixes`

**Issues**
1. **Major — Dependency Inversion / Hidden Semantic Coupling:** [add_line_controller.dart](/C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L623) reconstructs the post-confirm position by assuming the new leaf is `result.insertedMoveIds.last`, while [line_persistence_service.dart](/C:/code/draftable/chess-3/src/lib/services/line_persistence_service.dart#L13) exposes only a raw `insertedMoveIds` list. That makes the controller depend on an implicit ordering contract from the persistence/repository layer instead of an explicit abstraction. The feature now works only because inserts are returned in leaf order; if persistence ever batches, reorders, or changes its return shape, the controller will reload the wrong node and the preserved-pill behavior breaks. Fix by moving that knowledge into the persistence boundary: add an explicit `newLeafMoveId` (or similar) to `PersistResult` and have `LinePersistenceService` return the exact reload target.

2. **Minor — Single Responsibility / File Size / DRY:** The modified production files are already well past the 300-line smell threshold: [add_line_controller.dart](/C:/code/draftable/chess-3/src/lib/controllers/add_line_controller.dart#L111) is 716 lines and [add_line_screen.dart](/C:/code/draftable/chess-3/src/lib/screens/add_line_screen.dart#L43) is 595 lines. The modified test files are much larger again: [add_line_controller_test.dart](/C:/code/draftable/chess-3/src/test/controllers/add_line_controller_test.dart#L2378) is 2222 lines and [add_line_screen_test.dart](/C:/code/draftable/chess-3/src/test/screens/add_line_screen_test.dart#L2294) is 1917 lines. This change adds more branching/state-restoration behavior to the controller and a large amount of repeated confirm/setup flow in tests, which makes the architecture harder to read from code alone and raises the cost of future changes. Fix by extracting a small post-confirm reload helper/policy from the controller and scenario helpers/builders for the repeated `seed -> play -> confirm -> branch/undo` test flows.