# CT-54: Test Notes

## Test files modified

### `src/test/controllers/add_line_controller_test.dart`

**Updated existing tests (Step 7):**

1. **`undoNewLine` > "is a no-op when generation does not match"** (~line 1302): Updated to account for post-confirm position being at the e5 leaf instead of kInitialFEN. The second line is now played by navigating back to pill 0 (e4) and branching with d5, rather than playing d4/d5 from kInitialFEN. Assertions updated from 4 moves to 3 (e4 is shared between lines).

**New tests added (Step 8) -- group `CT-54: Pills persist after confirm`:**

2. **"pills persist after confirm (root start), all marked saved"**: Seeds empty repertoire, plays e4/e5, flips, confirms. Verifies pills list has 2 pills both with `isSaved: true`, `hasNewMoves` is false, `isExistingLine` is true, and `currentFen` is the e5 FEN (not kInitialFEN).

3. **"pills persist after confirm (mid-tree start)"**: Seeds e4/e5/Nf3, creates controller with `startingMoveId` = Nf3, plays Nc6, flips, confirms. Verifies pills show the full path (e4, e5, Nf3, Nc6) all saved, and `currentFen` is the Nc6 FEN.

4. **"board position preserved after confirm (preMoveFen matches leaf)"**: Verifies both `currentFen` and `preMoveFen` match the last confirmed move's FEN.

5. **"branching after confirm"**: After confirming e4/e5, navigates to pill 0 (e4) and plays d5 (diverging). Verifies pills show e4 (saved) and d5 (unsaved), and `hasNewMoves` is true.

6. **"confirm button disabled after confirm (hasNewMoves is false)"**: Confirms e4, verifies `hasNewMoves` is false post-confirm.

7. **"second confirm after branching"**: Confirms e4/e5, branches to e4/d5, confirms again. Verifies pills persist showing e4/d5 both saved. Verifies DB has 3 moves total (e4 shared).

8. **"undo after confirm resets to original starting position (root start)"**: Confirms e4/e5 then undoes. Verifies state resets to empty pills and kInitialFEN. Verifies DB has no moves.

9. **"undo after confirm resets to original starting position (mid-tree start)"**: Seeds e4/e5/Nf3, starts from Nf3, plays Nc6, confirms, then undoes extension. Verifies state resets to the Nf3 position (not kInitialFEN) with existing path pills e4/e5/Nf3.

### `src/test/screens/add_line_screen_test.dart`

**Updated existing tests (Step 7):**

1. **"Line saved snackbar is dismissed when user plays first move of new line"** (~line 1581): Updated to play the divergent move (e5) from the post-confirm position (e4 leaf FEN, black to move) instead of d4 from kInitialFEN.

2. **"Line extended snackbar is dismissed when user plays first move of new line"** (~line 1607): Updated to navigate back to pill 0 (e4 position) and play d5 from there, since post-confirm position is now at the e5 leaf (white to move) rather than the startingMoveId position.

**New tests added (Step 9) -- group `CT-54: Post-confirm persistence`:**

3. **"pills visible after confirm"**: Confirms a new line (e4), verifies the e4 pill is still rendered.

4. **"Existing line label visible after confirm"**: Confirms e4, verifies the "Existing line" info label appears.

5. **"confirm button disabled after confirm"**: Confirms e4, verifies the Confirm button's `onPressed` is null.

6. **"undo snackbar coexists with pills after confirm"**: Confirms e4, verifies both the "Line saved" snackbar and the e4 pill are visible simultaneously.

7. **"undo snackbar dismissed on new variation after confirm"**: Confirms an extension (e4->e5), navigates back to e4 pill, plays d5 (divergent move). Verifies the snackbar is dismissed when `hasNewMoves` transitions false-to-true.

## Acceptance criteria coverage

| Acceptance Criterion | Test Coverage |
|---|---|
| After confirm, pills remain visible and board stays at current position | Controller tests 2, 3, 4; Screen test 3 |
| A visual indicator shows that the displayed line is already in the repertoire | Controller test 2 (`isExistingLine`); Screen test 4 ("Existing line" label) |
| Confirm button is disabled since all moves are saved | Controller test 6; Screen test 5 |
| User can tap a previous pill, play a different move, and confirm the new branch | Controller tests 5, 7 |
| Undo feedback does not clear the persistent pill state | Screen tests 6, 7 |
| Update `features/add-line.md` Entry Flow | Not testable automatically (spec document change) |
| Update `features/add-line.md` "already saved" indicator behavior | Not testable automatically (spec document change) |
| Update `features/add-line.md` tap previous pill + new variation | Not testable automatically (spec document change) |

## Acceptance criteria NOT testable automatically

- **Spec file updates** (`features/add-line.md`): Three acceptance criteria relate to updating the feature spec document. These are documentation changes, not code behavior, and cannot be verified with automated tests. They should be reviewed manually during PR review.
