# CT-1.3 Discovered Tasks

No new tasks discovered. All follow-up items map to existing work:

- **HomeScreen Riverpod migration** — already scoped to CT-1.4
- **Widget test for entirely-auto-played lines** — minor test gap, not a standalone task
- **Board move flash on incorrect moves** — inherent limitation of ChessboardWidget API, documented in plan Risk #2. A "validate before play" callback could be added to ChessboardWidget in a future task if the UX proves problematic.
