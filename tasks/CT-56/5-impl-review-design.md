- **Verdict** — `Approved with Notes`
- **Issues**
  1. **Major — Hidden Coupling / DRY:** The transposition feature depends on three separate representations of the same path state staying perfectly aligned: pill construction in [add_line_controller.dart](C:/code/draftable/chess-1/src/lib/controllers/add_line_controller.dart#L228), aggregate-name computation in [add_line_controller.dart](C:/code/draftable/chess-1/src/lib/controllers/add_line_controller.dart#L782), and active-path snapshot building in [add_line_controller.dart](C:/code/draftable/chess-1/src/lib/controllers/add_line_controller.dart#L815), all feeding the primitive-heavy API in [line_entry_engine.dart](C:/code/draftable/chess-1/src/lib/services/line_entry_engine.dart#L327). This is semantic and temporal coupling: a future change to pill ordering, pending-label overlay, or focus semantics can silently break transposition classification unless all three loops and the call contract are updated together. Fix by extracting a single `ActivePathSnapshot`/`EffectivePath` projection and reusing it for pills, display name, and transposition lookup instead of recomputing the same traversal in multiple places.
  2. **Major — Single Responsibility / Abstraction Boundary:** `LineEntryEngine.findTranspositions()` is doing domain lookup and presentation formatting at the same time by returning preformatted `aggregateDisplayName` and `pathDescription` from [line_entry_engine.dart](C:/code/draftable/chess-1/src/lib/services/line_entry_engine.dart#L367). That makes a pure business-logic service depend on UI-facing wording/formatting decisions, so localization or display changes now require changes in the engine. Fix by returning a domain-level match model (`moveId`, classification, maybe path IDs/labels) and mapping that to display text in the controller or a dedicated presenter/view-model layer.
  3. **Minor — File Size / SRP:** The modified production files are all past the 300-line smell threshold: [add_line_controller.dart](C:/code/draftable/chess-1/src/lib/controllers/add_line_controller.dart) is 798 lines, [add_line_screen.dart](C:/code/draftable/chess-1/src/lib/screens/add_line_screen.dart) is 776, and [line_entry_engine.dart](C:/code/draftable/chess-1/src/lib/services/line_entry_engine.dart) is 335. The modified test files are also very large: [add_line_controller_test.dart](C:/code/draftable/chess-1/src/test/controllers/add_line_controller_test.dart) is 2458 lines, [add_line_screen_test.dart](C:/code/draftable/chess-1/src/test/screens/add_line_screen_test.dart) is 2115, and [line_entry_engine_test.dart](C:/code/draftable/chess-1/src/test/services/line_entry_engine_test.dart) is 1175. The feature works, but the architecture is getting harder to understand from code alone because each new concern is being added to already oversized modules. Fix by extracting a transposition-specific collaborator/widget and moving the new test setup/assertion patterns into dedicated helpers or separate test files.