# CT-56 Test Notes

## Test files modified

### `src/test/services/line_entry_engine_test.dart`

Added `group('findTranspositions', ...)` with 10 test cases:

| Test | Covers |
|------|--------|
| returns empty list when position is unique | No false positives for non-transposing lines |
| detects transposition via different move order | Core transposition detection (e4 d5 d4 vs d4 d5 e4) |
| excludes moves on the same path from matches | Same-path filtering: a move should not match itself |
| same-opening classification when paths share a label | AC: same-opening when labels overlap |
| same-opening classification when active path has no labels | AC: same-opening when either path has no labels |
| same-opening classification when match path has no labels | AC: same-opening when either path has no labels |
| cross-opening classification when labels differ | AC: cross-opening when both have labels with no overlap |
| same-opening matches sorted before cross-opening | AC: same-opening listed first |
| works for buffered moves (position not in tree) | AC: works for buffered (new) moves |
| classification reflects pending labels | AC: classification uses effective labels from controller |
| populates aggregateDisplayName and pathDescription | Warning displays alternative path and labels |

### `src/test/controllers/add_line_controller_test.dart`

Added `group('Transposition detection', ...)` with 8 test cases:

| Test | Covers |
|------|--------|
| transpositionMatches populated after move reaching existing position | AC: system checks movesByPositionKey after each move |
| transpositionMatches cleared after take-back to non-transposition position | AC: warning disappears when position changes (take-back) |
| transpositionMatches updated on pill tap | AC: warning updates on pill navigation; active-path truncation correctness |
| transpositionMatches recomputed after updateLabel changes classification | AC: same-opening vs cross-opening reclassification after label edit |
| transpositionMatches recomputed after updateBufferedLabel | AC: classification reflects buffered move labels |
| transpositionMatches preserved after flipBoard | Board flip does not clear transposition state |
| transpositionMatches empty at initial position | Initial position is never flagged as a transposition |
| transpositionMatches works for followed (existing) moves | AC: works for followed (existing) moves, not just buffered |

### `src/test/screens/add_line_screen_test.dart`

Added `group('CT-56: Transposition warning', ...)` with 5 widget test cases:

| Test | Covers |
|------|--------|
| warning appears when transposition exists and disappears on next move | AC: inline warning shown below pills; disappears when position changes |
| warning shows Reroute button for same-opening matches only | AC: same-opening matches show Reroute button |
| warning does not show Reroute button for cross-opening matches | AC: cross-opening matches do not show Reroute button |
| warning disappears after take-back | AC: warning disappears on take-back |
| warning does not block user from continuing to add moves | AC: non-blocking, informational only |

## Acceptance criteria coverage

| AC | Covered by |
|----|------------|
| After each move, system checks movesByPositionKey | Controller test: "populated after move" |
| Moves on same path excluded | Engine test: "excludes moves on same path" |
| Inline warning shown below pills | Widget test: "warning appears" |
| Warning shows alternative paths and labels | Engine test: "populates aggregateDisplayName and pathDescription" |
| Warning disappears when position changes | Widget tests: "disappears on next move", "disappears after take-back" |
| Warning does not block user | Widget test: "does not block user" |
| Works for both followed and buffered moves | Engine test: "works for buffered moves"; Controller test: "works for followed moves" |
| Same-opening matches listed before cross-opening | Engine test: "same-opening sorted before cross-opening" |
| Same-opening matches show Reroute; cross-opening do not | Widget tests: "shows Reroute for same-opening", "no Reroute for cross-opening" |
| Same-opening if shared label or either has no labels | Engine tests: 3 same-opening classification tests |
| Cross-opening if both have labels and none overlap | Engine test: "cross-opening when labels differ" |
| Unit tests for transposition detection logic | Engine and controller test groups |
| Unit tests for same-opening vs cross-opening classification | Engine tests: 4 classification tests |
| Widget test confirming warning appears/disappears | Widget test group |

## Acceptance criteria NOT testable automatically

| AC | Reason |
|----|--------|
| Update `features/add-line.md` with Transposition Detection section | Spec file update -- verified by manual review |
| Update `features/line-management.md` with transposition note | Spec file update -- verified by manual review |
