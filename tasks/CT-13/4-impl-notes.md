# CT-13: Implementation Notes

## Files Modified

- **`src/lib/screens/settings_screen.dart`** — Added `showCheckmark: false` and `side: BorderSide(...)` to the `ChoiceChip` in `_buildPieceSetPicker`. The selected chip now shows a primary-colored 2px border instead of a checkmark icon; unselected chips get a subtle 1px `outlineVariant` border.

## Deviations from Plan

None. The implementation follows the revised plan exactly.

## New Tasks / Follow-up Work

None discovered.
