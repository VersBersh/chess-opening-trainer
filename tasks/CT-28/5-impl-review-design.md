- **Verdict** — `Approved with Notes`
- **Issues**
1. **Major — Dependency Inversion / Hidden Coupling**: [`src/lib/services/drill_engine.dart:217`](C:/code/misc/chess-trainer-8/src/lib/services/drill_engine.dart:217) directly reaches into cache internals (`_treeCache.movesById[parentMoveId]!.fen`) and combines that with cache-specific normalization/lookup (`normalizePositionKey`, `getChildrenAtPosition`). This couples `DrillEngine` to `RepertoireTreeCache` storage details and the “FEN is post-move” rule, so either module changing can force edits in both. It also hard-crashes on inconsistent tree data due to `!`. Suggested fix: encapsulate this in `RepertoireTreeCache` (for example, `isAlternativeMoveAtExpectedPosition(expectedMove, san)`), and handle missing parent defensively inside the cache boundary.
2. **Minor — File Size (Clean Code smell)**: [`src/test/services/drill_engine_test.dart`](C:/code/misc/chess-trainer-8/src/test/services/drill_engine_test.dart) is **948 lines**. Per your criterion, this should be flagged. It is getting hard to navigate and mixes many behaviors in one unit. Suggested fix: split by concern (for example `intro_moves_test.dart`, `submit_move_test.dart`, `scoring_test.dart`, `session_progress_test.dart`) and keep shared builders in a small test helper file.