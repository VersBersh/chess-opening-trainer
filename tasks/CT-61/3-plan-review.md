**Verdict** — `Needs Revision`

**Issues**
1. **Major — Step 2:** The proposed test does not verify the risky behavior the plan depends on. In [src/test/screens/add_line_screen_test.dart:388](/C:/code/draftable/chess-4/src/test/screens/add_line_screen_test.dart), the existing test only checks `TextButton.onPressed == null`, and adding `find.byTooltip('Tap a move to edit its label')` would only prove that a `Tooltip` widget exists in the tree. It would not prove that a long-press on the disabled Label button actually shows the tooltip, which is the key assumption behind Step 1. The codebase examples in [src/test/screens/home_screen_test.dart:1257](/C:/code/draftable/chess-4/src/test/screens/home_screen_test.dart) and [src/lib/widgets/move_tree_widget.dart:328](/C:/code/draftable/chess-4/src/lib/widgets/move_tree_widget.dart) cover normal tooltip presence, not a tooltip wrapped around a disabled Material button. Fix: update Step 2 to long-press the disabled Label control and assert the tooltip text appears after a pump, optionally keeping the structural `find.byTooltip(...)` assertion as a secondary check.

2. **Minor — Step 1:** The implementation approach is compatible with the current action bar in [src/lib/screens/add_line_screen.dart:886](/C:/code/draftable/chess-4/src/lib/screens/add_line_screen.dart), and it matches `canEditLabel` in [src/lib/controllers/add_line_controller.dart:979](/C:/code/draftable/chess-4/src/lib/controllers/add_line_controller.dart) plus the feature spec in [features/add-line.md:58](/C:/code/draftable/chess-4/features/add-line.md). But the example duplicates the entire `TextButton.icon` in both branches. Fix: keep the plan’s conditional wrapping, but note that the button itself should ideally be built once and wrapped only when disabled to reduce duplication and future drift.