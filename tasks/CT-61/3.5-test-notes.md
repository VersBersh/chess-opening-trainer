# CT-61: Test Notes

## Test files modified

- `src/test/screens/add_line_screen_test.dart` -- two new `testWidgets` added after the existing `'label button disabled when no pill focused'` test (around line 401).

## Tests added

### 1. `'disabled label button shows tooltip on long-press'`

Covers:
- **AC: Disabled Label button shows a tooltip on long-press explaining the requirement.** Seeds an empty repertoire (no moves, no pill focused) so the Label button is disabled. Asserts a `Tooltip` widget wraps the `TextButton`. Checks the `message` property equals `'Play or select a move to edit labels'`. Long-presses the button and verifies the tooltip text appears on screen.
- **AC: Tooltip text is clear and actionable.** Verified by asserting the exact message string.
- **AC: Widget test verifying tooltip is present when button is disabled.** This is the widget test itself.

### 2. `'enabled label button has no tooltip wrapper'`

Covers:
- Negative case: when a pill IS focused (button enabled), no `Tooltip` widget wraps the Label button. Ensures the tooltip is conditional and only present in the disabled state.

## Acceptance criteria coverage

| Criterion | Covered | Test |
|-----------|---------|------|
| Disabled Label button shows tooltip on long-press | Yes | Test 1 |
| Tooltip text is clear and actionable | Yes | Test 1 (exact string assertion) |
| Widget test verifying tooltip is present when disabled | Yes | Test 1 |

All acceptance criteria are covered by automated widget tests. No criteria required manual testing.
