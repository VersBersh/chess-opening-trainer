# 3.5 Test Notes — CT-61

## Test files modified

- `src/test/screens/add_line_screen_test.dart`

## Tests added

### 1. `disabled Label button is wrapped in Tooltip with explanation`

Structural check: seeds an empty repertoire (no pills focused), pumps the AddLineScreen, confirms the Label button is disabled, then asserts that a `Tooltip` widget with message `'Tap a move to edit its label'` exists in the widget tree via `find.byTooltip(...)`.

### 2. `disabled Label button tooltip appears on long-press`

Behavioral check: seeds an empty repertoire, pumps the screen, long-presses the disabled Label button, pumps with a 2-second duration to allow the tooltip overlay to animate in, then asserts the tooltip text `'Tap a move to edit its label'` is rendered on screen.

## Acceptance criteria coverage

| Criterion | Covered | Notes |
|-----------|---------|-------|
| Disabled Label button shows a tooltip on long-press explaining the requirement | Yes | Test 2 |
| Tooltip text is clear and actionable | Yes | Both tests assert the exact message |
| Widget test verifying tooltip is present when button is disabled | Yes | Tests 1 and 2 |

All acceptance criteria are covered by automated tests.
