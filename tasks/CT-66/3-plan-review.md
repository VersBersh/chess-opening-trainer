**Verdict** — `Needs Revision`

**Issues**
1. Major — Step 1 keeps the aggregate display name as a dynamic widget above the board, which conflicts with the feature spec and the existing wide-layout direction. The current narrow layout already renders that banner above the board in [_buildNarrowContent](/C:/code/draftable/chess-4/src/lib/screens/add_line_screen.dart#L487), but the spec explicitly says only the static app bar may appear above the board and that the aggregate name must be shown below the board instead ([add-line.md](/C:/code/draftable/chess-4/features/add-line.md#L13), [add-line.md](/C:/code/draftable/chess-4/features/add-line.md#L122)). Wrapping that banner in `AnimatedSize` makes the immediate keyboard fix work, but it also bakes in the out-of-spec structure instead of moving toward the established contract. Suggested fix: revise Step 1 to move the aggregate display name below the board on narrow layout, matching the wide layout’s pattern, and then only collapse the board container when `isKeyboardOpen && _isLabelEditorVisible`.

2. Major — Step 3 does not actually verify the banner behavior that Step 1 changes. In the proposed tests, the repertoire is seeded without labels (`['e4', 'e5']`), so `state.aggregateDisplayName` stays empty and the banner branch at [_buildNarrowContent](/C:/code/draftable/chess-4/src/lib/screens/add_line_screen.dart#L501) never renders. That means the plan’s claimed “display name banner should also collapse” requirement from [1-context.md](/C:/code/draftable/chess-4/tasks/CT-66/1-context.md#L50) is untested. Suggested fix: either remove banner work from the implementation plan, or add at least one test with a labeled path / `startingMoveId` so `displayName.isNotEmpty` is true and assert the banner also collapses or no longer exists above the board.

3. Minor — Step 3’s helper guidance is incomplete for tests 3b and 3c. The existing Add Line test helper has no `MediaQuery` support ([add_line_screen_test.dart](/C:/code/draftable/chess-4/src/test/screens/add_line_screen_test.dart#L157)), and the drill reference uses two patterns: a static `buildTestApp` for fixed insets and a separate `ValueListenableBuilder` helper for runtime inset changes ([drill_filter_test.dart](/C:/code/draftable/chess-4/src/test/screens/drill_filter_test.dart#L286), [drill_filter_test.dart](/C:/code/draftable/chess-4/src/test/screens/drill_filter_test.dart#L797)). The plan recommends only extending `buildTestApp`, but that alone does not describe how 3b/3c will preserve screen state while `viewInsets` changes. Suggested fix: explicitly add a second helper for the keyboard-toggle cases, mirroring the drill test’s `ValueNotifier<EdgeInsets>` pattern.

4. Minor — Step 3a’s final assertion should target the label editor’s `TextField`, not `InlineLabelEditor` itself, for hit-testing. `InlineLabelEditor` is a `StatefulWidget` wrapper in [inline_label_editor.dart](/C:/code/draftable/chess-4/src/lib/widgets/inline_label_editor.dart#L10); the hittable element is the `TextField` it builds ([inline_label_editor.dart](/C:/code/draftable/chess-4/src/lib/widgets/inline_label_editor.dart#L138)). Suggested fix: assert `find.byType(TextField).hitTestable()` or a more specific finder for the label field instead of treating the `InlineLabelEditor` widget as the hittable target.