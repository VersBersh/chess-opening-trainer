- **Verdict** — Approved
- **Issues** — None.

Plan checks out against the current codebase: referenced files, APIs, types, and line-level claims are accurate; the proposed `_formatNextDue` change is minimal, correctly scoped, and consistent with existing patterns; ordering/dependencies are fine; and the grep verification step is appropriate for regression risk.