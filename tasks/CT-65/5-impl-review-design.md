- **Verdict** — `Needs Fixes`
- **Issues**
  1. **Major — Dependency Inversion / Encapsulation:** [src/lib/widgets/move_tree_widget.dart:13](/C:/code/draftable/chess-4/src/lib/widgets/move_tree_widget.dart#L13), [src/lib/widgets/move_tree_widget.dart:25](/C:/code/draftable/chess-4/src/lib/widgets/move_tree_widget.dart#L25), [src/test/widgets/move_tree_widget_test.dart:1090](/C:/code/draftable/chess-4/src/test/widgets/move_tree_widget_test.dart#L1090). The new `kTreeRowPaddingLeft`, `kIndentPerLevel`, `kMaxIndentDepth`, and `computeTreeIndent()` symbols are public solely so tests can call them. That makes the production module’s API depend on test needs rather than the widget’s design, and it exposes layout internals as if they were supported extension points. This weakens the module boundary and gives future callers a way to couple themselves to a private rendering detail. Fix by keeping the indentation policy private to the widget file and verifying it through widget behavior, or by extracting a real indentation policy abstraction only if multiple production callers need it.
  2. **Major — Hidden Semantic Coupling in Tests:** [src/test/widgets/move_tree_widget_test.dart:1056](/C:/code/draftable/chess-4/src/test/widgets/move_tree_widget_test.dart#L1056). The new widget test discovers indentation by iterating every `Padding` in the subtree and filtering on `EdgeInsets.right == 8.0`. That test is coupled to the current widget composition, not the user-visible contract. A harmless refactor such as using `Container`, `EdgeInsetsDirectional`, or a different internal padding structure would break the test without changing behavior. Fix by targeting specific rows with keys, or by asserting rendered position/offset for those rows instead of mining incidental `Padding` widgets.
  3. **Minor — File Size / Single Responsibility:** [src/lib/widgets/move_tree_widget.dart](/C:/code/draftable/chess-4/src/lib/widgets/move_tree_widget.dart), [src/test/widgets/move_tree_widget_test.dart](/C:/code/draftable/chess-4/src/test/widgets/move_tree_widget_test.dart). Both modified files are already over the 300-line smell threshold (`move_tree_widget.dart` is 373 lines; `move_tree_widget_test.dart` is 1117 lines), and this change adds more responsibilities to each. The production file now mixes tree flattening, notation formatting, indentation policy, widget orchestration, and tile rendering; the test file keeps accumulating unrelated scenarios into one monolith. Fix by splitting indentation policy/tests into focused modules, and by breaking the test suite into smaller files by concern.